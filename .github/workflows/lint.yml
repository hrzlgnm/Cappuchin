name: "Reusable lint workflow"

on:
  workflow_call:

jobs:
  Lint:
    runs-on: ubuntu-latest
    container:
      image: "ghcr.io/hrzlgnm/cappuchin-github-void-linux-glibc-clang:v1@sha256:06c698255ad9c042416b26dd6654776c1d0edf150dccee89df0cc2df1b8d6ea4"
    steps:
      - name: Checkout
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0

      - name: 📐 Check cmake format
        run: gersemi --check --diff .

      - name: 📐 Check clang-format
        run: cmake -D FORMAT_COMMAND=clang-format -P cmake/lint.cmake
